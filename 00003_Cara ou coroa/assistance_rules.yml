---
rules:
- when:
    error_contains: 'IndentationError:'
  then: parece que há um problema com a tabulação, não é mesmo? Certifique-se de que
    `if` esteja recuado à direita em relação ao `def`, e que cada um dos ramos também
    esteja recuado para a direita com respeito ao `if`
